# we are going to create an Bulk file renameing program

import os

def rename():
    i = 0
    path = "/sdcard/vipc/"
    #now we create an loop 
    for filename in os.listdir(path):
        to = "hello" + str(i) + ".txt"
        fro =path + filename
        to =path + to
        os.rename(fro, to)
        i += 1       
if __name__ == '__main__':
    rename()
    
    #lets se which file we are going to rename
    # lets run the program....,.......
    #lets seee the results
